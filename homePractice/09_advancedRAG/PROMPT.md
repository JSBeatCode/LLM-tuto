//

너는 llm 및 ai api 전문가야. 
내가 llm 및 ai api 관련 코드가 담긴 '.ipynb' 파일을 주면 너가 보고 분석한 후에 정리해 줄 수 있어?
내가 코드파일을 줄테니 이 코드가 전체적으로 어떤 것을 하는지 주요 기능들 요약 정리 해줘.

//

//
이 코드를 공부할거야. 
이 코드를 공부할때에 주제로 나누어서 각 주제별로 하나의 '.py'파일로 만들어서 각각 실행하는게 좋은지, 아니면 나누지 않고 하나의 '.py'로 만들어서 실행하는게 좋은 코드인지 먼저 판단해줘. 일단 나누지 말지 판단만 해줘.

//

주제로 나눌 수 있는 코드면 이 코드에서 배울 수 있는 것을 주제로 나누고 싶어. 배울 수 있는 주제를 무엇 무엇으로 나눌 수 있어? 코드에 나온 순차적인 순서로 주제를 나눠주되, 묶을 수 있는 것은 좀 묶어줄 수 있어?

//

[요청사항]
아까 너가 제시한 주제들 중 
'
Topic 1 — Gemini API 설정 & LangChain 연동

API 키 등록, 키 유효성 검증, ChatGoogleGenerativeAI 모델 초기화
→ "OpenAI 대신 Gemini를 LangChain에 붙이는 법"
' 
을 코딩을 하고 싶어. 

---
[코딩 참고사항]
1.이 주제로 실행 가능하도록 하나의 '.py' 파일을 만들고 싶어.
2.내가 준 .ipynb 파일의 코드를 최대한 활용해줘.
3.모든 API_KEY나 API 관련 데이터를 불러오는 방식은 '.env' 파일을 생성하고 'from dotenv import load_dotenv'라는 코드를 활용해줘.
4.참고로 파이썬 3.11 버전을 쓰고 있어.
5.코드를 실행하면서 생성하게 되는 모든 파일과 chrome db 등은 '.py'와 동일한 위치에 있게 해줘.
6.과도한 loop 코드나 과도한 batch로 인해 TPM 호출 한도에 안 걸리게 코드 짜줘.
//

[요청사항]
이제 내가 업로드 한 파일을 기반으로 재코딩을 하고 싶어. 

---
[코딩 참고사항]
1.이 주제로 실행 가능하도록 하나의 '.py' 파일을 만들고 싶어.
2.내가 준 .ipynb 파일의 코드를 최대한 활용해줘.
3.모든 API_KEY나 API 관련 데이터를 불러오는 방식은 '.env' 파일을 생성하고 'from dotenv import load_dotenv'라는 코드를 활용해줘.
4.참고로 파이썬 3.11 버전을 쓰고 있어.
5.코드를 실행하면서 생성하게 되는 모든 파일과 chrome db 등은 '.py'와 동일한 위치에 있게 해줘.
6.과도한 loop 코드나 과도한 batch로 인해 TPM 호출 한도에 안 걸리게 코드 짜줘.